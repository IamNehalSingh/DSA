void insertatend(int value)
{
    Node*ptr=new NOde();
    ptr->data=value;
    ptr->link=NULL;

    if (head==NULL)
    cout<<"linked is empty";

    else
    {
        Node*temp=head;
        while(temp->link!=NULL)
        {
            temp=temp->link;
        }
        temp->link=ptr;
    


}